# Graphify Diagrams

## 1. Generate and play sequence

```mermaid
sequenceDiagram
    actor User
    participant App as App.jsx
    participant Algo as algorithms.js
    participant Hook as usePlayback
    participant State as stepState.js
    participant Canvas as GraphCanvas/TreeCanvas
    participant Panel as Sidebar

    User->>App: Select algorithm and click Generate Steps
    App->>Algo: bfsSteps/dfsSteps/dijkstraSteps/treeSteps(model)
    Algo-->>App: steps[] event log
    App->>Hook: set steps and reset cursor
    Hook->>State: derivePlaybackState(steps, 0)
    State-->>Canvas: empty playback state
    State-->>Panel: empty insights
    User->>Hook: Click Play or Next
    Hook->>Hook: Advance stepIndex
    Hook->>State: Replay steps before cursor
    State-->>Canvas: visited, active edges, path, current state
    State-->>Panel: queue/stack/distances/timeline
    Canvas-->>User: Animated SVG update
    Panel-->>User: Explanation and data update
```

## 2. Graph editing sequence

```mermaid
sequenceDiagram
    actor User
    participant Canvas as GraphCanvas
    participant App as App.jsx
    participant Model as graphModel.js
    participant React as React state

    User->>Canvas: Click empty SVG position
    Canvas->>Canvas: Convert screen point using SVG matrix and pan/zoom
    Canvas->>App: onCanvasClick(x, y)
    App->>Model: addGraphNode(graph, x, y, value)
    Model-->>App: New graph object
    App->>React: setGraph(newGraph)
    App->>React: resetExecution()
    React-->>Canvas: Re-render nodes
```

## 3. Proposed WebSocket collaboration sequence

This is a future design, not current functionality.

```mermaid
sequenceDiagram
    actor A as User A
    actor B as User B
    participant CA as Client A
    participant WS as WebSocket server
    participant Store as Room state/event store
    participant CB as Client B

    CA->>WS: CONNECT + authenticate
    CB->>WS: CONNECT + authenticate
    CA->>WS: JOIN room-42
    WS-->>CA: snapshot revision 17
    CB->>WS: JOIN room-42
    WS-->>CB: snapshot revision 17
    A->>CA: Add node
    CA->>WS: command node.add, baseRevision 17
    WS->>WS: Validate command and permissions
    WS->>Store: Apply command, create revision 18
    Store-->>WS: event node.added revision 18
    WS-->>CA: accepted event revision 18
    WS-->>CB: broadcast event revision 18
    CA->>CA: Apply once
    CB->>CB: Apply once
    CB->>WS: command based on revision 18
    WS-->>CB: accepted event revision 19
```

## 4. Component/class relationship diagram

The code is functional React rather than class-based OO. This diagram uses a class-style view to show responsibilities and data dependencies.

```mermaid
classDiagram
    class App {
      +page
      +mode
      +graph
      +tree
      +steps
      +algorithm
      +runSelectedAlgorithm()
      +resetExecution()
      +handleGraphCanvasClick(x,y)
    }

    class UsePlayback {
      +isPlaying
      +speed
      +stepIndex
      +playbackState
      +stepForward()
      +stepBackward()
      +reset()
    }

    class GraphModel {
      +createInitialGraph()
      +addGraphNode()
      +moveGraphNode()
      +removeGraphNode()
      +addGraphEdge()
      +removeGraphEdge()
      +graphToAdjacency()
    }

    class TreeModel {
      +createInitialTree()
      +addTreeNode()
      +connectTreeNode()
      +removeTreeNode()
      +layoutTree()
    }

    class Algorithms {
      +bfsSteps()
      +dfsSteps()
      +dijkstraSteps()
      +inorderSteps()
      +preorderSteps()
      +postorderSteps()
    }

    class StepState {
      +derivePlaybackState(steps,index)
    }

    class GraphCanvas {
      +renderGraph()
      +handlePointerMove()
      +handleBackgroundClick()
    }

    class TreeCanvas {
      +renderTree()
      +layoutTree()
    }

    class Sidebar {
      +renderInsights()
      +renderTimeline()
    }

    App --> UsePlayback : uses
    App --> GraphModel : updates graph
    App --> TreeModel : updates tree
    App --> Algorithms : generates steps
    UsePlayback --> StepState : derives state
    App --> GraphCanvas : passes props
    App --> TreeCanvas : passes props
    App --> Sidebar : passes props
    GraphCanvas --> GraphModel : emits edit callbacks
    TreeCanvas --> TreeModel : uses layout
```

## 5. Conceptual ER diagram

There is no relational database in the current project. This is a conceptual schema for the in-memory/local-storage data and a possible future backend. `Graph` and `Tree` are alternative workspace structures; a saved session may contain either or both.

```mermaid
erDiagram
    WORKSPACE ||--o| GRAPH : contains
    WORKSPACE ||--o| TREE : contains
    GRAPH ||--o{ GRAPH_NODE : has
    GRAPH ||--o{ GRAPH_EDGE : has
    GRAPH_NODE ||--o{ GRAPH_EDGE : from
    GRAPH_NODE ||--o{ GRAPH_EDGE : to
    TREE ||--o{ TREE_NODE : has
    TREE_NODE ||--o| TREE_NODE : left_child
    TREE_NODE ||--o| TREE_NODE : right_child
    WORKSPACE ||--o{ EXECUTION_STEP : records
    EXECUTION_STEP }o--o| GRAPH_NODE : references

    WORKSPACE {
      string id PK
      string mode
      int revision
      datetime updated_at
    }
    GRAPH {
      string id PK
      boolean directed
      boolean weighted
      boolean grid_mode
      int next_node_id
    }
    GRAPH_NODE {
      string id PK
      string graph_id FK
      string value
      float x
      float y
    }
    GRAPH_EDGE {
      string id PK
      string graph_id FK
      string from_node_id FK
      string to_node_id FK
      float weight
    }
    TREE {
      string id PK
      string root_node_id FK
      int next_node_id
    }
    TREE_NODE {
      string id PK
      string tree_id FK
      string value
      string color
      string left_child_id FK
      string right_child_id FK
    }
    EXECUTION_STEP {
      string id PK
      string workspace_id FK
      int sequence_number
      string type
      json payload
    }
```

## 6. WebSocket event model

```mermaid
flowchart LR
    Command[Client command] --> Validate[Schema and authorization validation]
    Validate --> Revision[Server assigns revision]
    Revision --> Store[Canonical room state]
    Store --> Event[Immutable domain event]
    Event --> Broadcast[Broadcast to room]
    Broadcast --> Apply[Client applies idempotently]
    Apply --> Gap{Revision gap?}
    Gap -- No --> Render[Render local state]
    Gap -- Yes --> Snapshot[Request fresh snapshot]
    Snapshot --> Render
```

Suggested event categories:

- `room.snapshot`
- `graph.node.added`
- `graph.node.moved`
- `graph.node.removed`
- `graph.edge.added`
- `graph.edge.removed`
- `execution.started`
- `execution.step`
- `execution.completed`
- `error`
