# RideShare Architecture and Diagrams

## System context diagram

```mermaid
flowchart LR
    P[Passenger browser] -->|HTTPS JSON + cookie| API[Express API]
    D[Driver browser] -->|HTTPS JSON + cookie| API
    API --> DB[(MongoDB)]
    API --> NOM[Nominatim geocoding]
    API --> OSRM[OSRM routing]
    API --> MAIL[Nodemailer / email provider]
```

## Component diagram

```mermaid
flowchart TB
    subgraph Client[React/Vite frontend]
        Router[React Router]
        Pages[Pages and components]
        Redux[Redux auth slice]
        Map[MapLibre map and mapApi cache]
    end

    subgraph Server[Node.js / Express backend]
        Index[Express bootstrap]
        Middleware[Cookie JWT middleware]
        Routes[Route modules]
        Handlers[Business handlers]
        Models[Mongoose models]
        Compute[Polyline + Haversine matching]
        Integrations[OSM / OSRM / email helpers]
    end

    DB[(MongoDB)]
    External[External APIs]

    Router --> Pages
    Pages --> Redux
    Pages --> Map
    Pages -->|HTTP| Index
    Index --> Middleware
    Middleware --> Routes
    Routes --> Handlers
    Handlers --> Models
    Handlers --> Compute
    Handlers --> Integrations
    Models --> DB
    Integrations --> External
```

## Booking sequence diagram

```mermaid
sequenceDiagram
    actor Passenger
    participant UI as React Search UI
    participant API as Express /book
    participant Map as Nominatim + OSRM
    participant DB as MongoDB
    participant Mail as Email provider
    actor Driver

    Passenger->>UI: Enter pickup, destination, date, time
    UI->>API: POST /book/search
    API->>Map: Geocode both addresses
    Map-->>API: Coordinates
    API->>Map: Calculate passenger route
    Map-->>API: Distance, duration, geometry
    API->>DB: Read offered routes
    API->>API: Filter date/time and test ordered intersections
    API-->>UI: Matching rides and fixed fare
    Passenger->>UI: Select ride and book
    UI->>API: POST /book/matchride with cookie
    API->>DB: Create userRoute(requested)
    API->>DB: Update route(pending + matchedUser)
    API->>Mail: Send request notification
    Mail-->>Driver: New ride request email
    API-->>UI: Booking request accepted
    Driver->>API: Accept or decline request
    API->>DB: Update route and userRoute statuses
    API-->>UI: Updated status on next HTTP read
```

## Driver offer sequence diagram

```mermaid
sequenceDiagram
    actor Driver
    participant UI as React Offer UI
    participant API as Express /offer
    participant Map as Nominatim + OSRM
    participant DB as MongoDB

    Driver->>UI: Enter source, destination, date, time
    UI->>API: POST /offer/postride with HTTP-only cookie
    API->>API: Verify token and driver role
    API->>Map: Geocode source and destination
    Map-->>API: Coordinates
    API->>Map: Calculate driving route
    Map-->>API: Encoded route geometry and metrics
    API->>API: Decode polyline into coordinates
    API->>DB: Create route(available)
    DB-->>API: Created route
    API-->>UI: Success
```

## Class diagram

```mermaid
classDiagram
    class User {
        +ObjectId _id
        +String fullName
        +Number mobileNumber
        +String passwordHash
        +String role
        +String salt
        +signup()
        +login()
    }

    class Driver {
        +ObjectId _id
        +String firstName
        +String lastName
        +String emailId
        +Number mobileNumber
        +String passwordHash
        +String drivingLicenseNumber
        +String vehicleName
        +String vehicleNumber
        +String role
        +signup()
        +login()
    }

    class Route {
        +ObjectId _id
        +ObjectId driverId
        +String source
        +String destination
        +String date
        +String rideStartTime
        +String distance
        +String time
        +RoutePoint[] routePath
        +String status
        +MatchedUser matchedUser
        +publish()
        +accept()
        +decline()
    }

    class UserRoute {
        +ObjectId _id
        +ObjectId userId
        +ObjectId rideId
        +String startLocation
        +String endLocation
        +String date
        +String time
        +Number rideCost
        +String status
        +request()
        +complete()
    }

    class Feedback {
        +ObjectId _id
        +ObjectId userId
        +ObjectId userRouteId
        +Number rating
        +String feedbackText
    }

    class RoutePoint {
        +Number lat
        +Number lng
    }

    User "1" --> "many" UserRoute : creates
    Driver "1" --> "many" Route : publishes
    Route "1" --> "many" UserRoute : selected by
    User "1" --> "many" Feedback : writes
    UserRoute "1" --> "many" Feedback : receives
    Route "1" *-- "many" RoutePoint : contains
```

## ER diagram

```mermaid
erDiagram
    USER ||--o{ USER_ROUTE : requests
    DRIVER ||--o{ ROUTE : publishes
    ROUTE ||--o{ USER_ROUTE : selected_by
    USER ||--o{ FEEDBACK : submits
    USER_ROUTE ||--o{ FEEDBACK : receives

    USER {
        ObjectId _id PK
        string fullName
        number mobileNumber UK
        string password
        string role
        string salt
    }
    DRIVER {
        ObjectId _id PK
        string firstName
        string lastName
        string emailId UK
        number mobileNumber UK
        string password
        string drivingLicenseNumber UK
        string vehicleNumber UK
        string role
    }
    ROUTE {
        ObjectId _id PK
        ObjectId driverId FK
        string source
        string destination
        string date
        string rideStartTime
        string status
        string routePath
        ObjectId matchedUserId FK
        ObjectId matchedUserRouteId FK
    }
    USER_ROUTE {
        ObjectId _id PK
        ObjectId userId FK
        ObjectId rideId FK
        string startLocation
        string endLocation
        string date
        string time
        number rideCost
        string status
    }
    FEEDBACK {
        ObjectId _id PK
        ObjectId userId FK
        ObjectId userRouteId FK
        number rating
        string feedbackText
    }
```

## Status transition diagram

```mermaid
stateDiagram-v2
    [*] --> Available: Driver publishes route
    Available --> Pending: Passenger requests route
    Pending --> Ongoing: Driver accepts
    Pending --> Available: Driver declines before ride time
    Pending --> Cancelled: Driver declines after cutoff
    Ongoing --> Completed: Passenger marks completed
    Available --> Cancelled: Route cancelled
    Completed --> [*]
```

## How WebSockets would fit

The current project does not implement WebSockets. It uses request/response HTTP and email notifications. A future implementation could add Socket.IO or native WebSocket support beside the Express server:

```text
Driver and passenger browsers
       | persistent WebSocket connection
       v
Socket gateway
       | authenticate JWT during handshake
       | join user:{id} and ride:{id} rooms
       v
Ride service + MongoDB
```

Useful events would be `ride:request-created`, `ride:accepted`, `ride:declined`, and `ride:completed`. The server should still persist the state in MongoDB first and emit an event after a successful update. HTTP endpoints remain useful for initial page loading, retries, refresh, and clients that are temporarily disconnected.

A good interview answer is: “WebSockets provide low-latency server push, while HTTP is request/response. In this project status changes currently become visible on a later HTTP fetch and the driver also receives email. I would add authenticated Socket.IO rooms for live status updates, but MongoDB would remain the source of truth.”
