# ChatApp Diagrams

These diagrams describe the current application. Mermaid diagrams can be rendered in VS Code extensions, GitHub, and many Markdown viewers.

## Component architecture

```mermaid
flowchart TB
    subgraph Client[React client]
        Main[main.jsx]
        App[App routes]
        Auth[AuthContext]
        Chat[ChatContext]
        Group[GroupContext]
        Pages[Home Login Profile]
        Components[Sidebar ChatContainer GroupContainer RightSidebar]
    end

    subgraph Server[Node.js server]
        Bootstrap[server.js]
        Routes[Express routes]
        Middleware[protectRoute]
        Controllers[User Message Group controllers]
        Models[Mongoose models]
        Presence[userSocketMap]
        Realtime[Socket.IO]
    end

    Mongo[(MongoDB)]
    Cloudinary[(Cloudinary)]

    Main --> Auth
    Auth --> Chat
    Chat --> Group
    Auth --> App
    App --> Pages
    Pages --> Components
    Auth -->|Axios JWT| Routes
    Chat -->|Axios REST| Routes
    Group -->|Axios REST| Routes
    Auth <-->|Socket connection| Realtime
    Chat <-->|message events| Realtime
    Routes --> Middleware
    Middleware --> Controllers
    Controllers --> Models
    Models --> Mongo
    Controllers --> Cloudinary
    Controllers --> Realtime
    Realtime --> Presence
```

## Login and authentication sequence

```mermaid
sequenceDiagram
    actor User
    participant Login as LoginPage
    participant API as Express /api/auth/login
    participant UserController as userController.login
    participant DB as MongoDB
    participant Client as AuthContext
    participant Socket as Socket.IO

    User->>Login: Submit email and password
    Login->>Client: login("login", credentials)
    Client->>API: POST credentials
    API->>UserController: Route controller
    UserController->>DB: findOne by email
    DB-->>UserController: User hash
    UserController->>UserController: bcrypt.compare
    UserController->>UserController: sign JWT with user id
    UserController-->>Client: token and userData
    Client->>Client: localStorage and Axios token header
    Client->>Socket: connect with userId query
    Socket-->>Client: getOnlineUsers
    Client-->>User: Render protected home page
```

## Direct chat sequence

```mermaid
sequenceDiagram
    actor Sender
    participant ChatUI as ChatContainer
    participant Context as ChatContext
    participant API as messageRoutes
    participant Auth as protectRoute
    participant Controller as messageController
    participant DB as MongoDB
    participant Socket as Socket.IO
    actor Receiver

    Sender->>ChatUI: Type and submit message
    ChatUI->>Context: sendMessage({ text })
    Context->>API: POST /api/messages/send/:receiverId
    API->>Auth: Verify token
    Auth->>DB: Load sender without password
    Auth-->>Controller: req.user
    Controller->>DB: Message.create
    DB-->>Controller: newMessage
    Controller->>Socket: Lookup receiver socket
    Socket-->>Receiver: newMessage
    Controller-->>Context: newMessage JSON
    Context-->>ChatUI: Append sender message
    Receiver->>Context: handleDirectMessage
    Context->>API: PUT /api/messages/mark/:messageId
```

## Group chat sequence

```mermaid
sequenceDiagram
    actor Admin
    participant UI as Sidebar
    participant API as groupRoutes
    participant Controller as groupController
    participant DB as MongoDB
    actor MemberA
    actor MemberB

    Admin->>UI: Select users and create group
    UI->>API: POST /api/group/create
    API->>Controller: createGroup
    Controller->>DB: Group.create(admin + members)
    DB-->>Controller: group
    Controller-->>UI: Created group
    MemberA->>UI: Send group message
    UI->>API: POST /api/group/send/:groupId
    Controller->>DB: Message.create(senderId + groupId)
    Controller->>DB: Load group members
    Controller-->>MemberB: Socket.IO newGroupMessage if connected
    Controller-->>MemberA: REST response with persisted message
```

## Class-style design diagram

This is a conceptual class diagram of the main runtime responsibilities. React contexts and Express controllers are modules/functions rather than traditional classes, so the diagram uses class notation to show responsibility and relationships.

```mermaid
classDiagram
    class AuthContext {
        +token
        +authUser
        +onlineUsers
        +socket
        +login(state, credentials)
        +logout()
        +checkAuth()
        +updateProfile(data)
        +connectSocket(user)
    }

    class ChatContext {
        +messages
        +users
        +selectedUser
        +unseenMessages
        +getUsers()
        +getMessages(id)
        +sendMessage(data)
        +subscribeToMessages()
        +handleDirectMessage(message)
        +handleGroupMessage(message)
    }

    class GroupContext {
        +groups
        +selectedGroup
        +fetchGroups()
        +createGroup(data)
        +renameGroup(id, name)
        +addMembers(id, members)
        +removeMember(id, member)
        +transferAdmin(id, admin)
        +leaveGroup(id)
        +deleteGroup(id)
    }

    class User {
        +ObjectId _id
        +String email
        +String fullName
        +String passwordHash
        +String profilePic
        +String bio
    }

    class Message {
        +ObjectId _id
        +ObjectId senderId
        +ObjectId receiverId
        +ObjectId groupId
        +String text
        +String image
        +Boolean seen
        +Date createdAt
    }

    class Group {
        +ObjectId _id
        +String name
        +ObjectId admin
        +ObjectId[] members
        +ObjectId[] messages
    }

    class UserController {
        +signup(request, response)
        +login(request, response)
        +checkAuth(request, response)
        +updateProfile(request, response)
    }

    class MessageController {
        +getUsersForSidebar(request, response)
        +getMessages(request, response)
        +sendMessage(request, response)
        +markMessageAsSeen(request, response)
    }

    class GroupController {
        +createGroup(request, response)
        +getGroupMessages(request, response)
        +sendGroupMessage(request, response)
        +renameGroup(request, response)
        +deleteGroup(request, response)
    }

    AuthContext --> ChatContext : provides socket and axios
    AuthContext --> GroupContext : provides auth state
    ChatContext --> MessageController : REST requests
    GroupContext --> GroupController : REST requests
    UserController --> User : reads/writes
    MessageController --> Message : reads/writes
    GroupController --> Group : reads/writes
    GroupController --> Message : creates group messages
    Message --> User : sender reference
    Message --> User : receiver reference
    Message --> Group : optional group reference
    Group --> User : admin and members
```

## Entity relationship diagram

```mermaid
erDiagram
    USER ||--o{ MESSAGE : sends
    USER ||--o{ MESSAGE : receives
    USER ||--o{ GROUP : administers
    GROUP }o--o{ USER : contains
    GROUP ||--o{ MESSAGE : owns

    USER {
        ObjectId _id PK
        string email UK
        string fullName
        string password
        string profilePic
        string bio
        datetime createdAt
        datetime updatedAt
    }

    MESSAGE {
        ObjectId _id PK
        ObjectId senderId FK
        ObjectId receiverId FK
        ObjectId groupId FK
        string text
        string image
        boolean seen
        datetime createdAt
        datetime updatedAt
    }

    GROUP {
        ObjectId _id PK
        string name
        ObjectId admin FK
        ObjectId[] members FK
        ObjectId[] messages FK
        datetime createdAt
        datetime updatedAt
    }
```

## ERD explanation

- One user can send many messages.
- One user can receive many direct messages.
- A message has either a direct recipient or a group id in the intended application flow.
- A user can administer many groups over time.
- A group contains many users, and a user can belong to many groups. MongoDB represents this many-to-many relationship with the `members` array on `Group`.
- A group can have many messages through `Message.groupId`.
- The `Group.messages` array is present in the schema but is not maintained by the current controllers. The actual group-message relationship is currently the `groupId` field on `Message`.

## Interview whiteboard order

When asked to draw the system, use this order:

1. Browser with React contexts and chat components.
2. REST arrow to Express routes and `protectRoute`.
3. Controllers to MongoDB and Cloudinary.
4. A separate Socket.IO connection from the browser to the server.
5. Server-side user-to-socket mapping.
6. Message flow: persist first, emit second, fetch history for recovery.
7. Add scaling notes: Redis adapter, rooms, indexes, pagination, and authorization.
